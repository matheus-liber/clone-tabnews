POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=local_user
POSTGRES_DB=local_db
POSTGRES_PASSWORD=local_password

PGHOST=localhost
PGPORT=5432
PGUSER=local_user
PGDATABASE=local_db
PGPASSWORD=local_password

DATABASE-URL=postgres://$PGUSER:$PGPASSWORD@$PGHOST:$PGPORT/$PGDATABASE

